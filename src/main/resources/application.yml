server:
  port: 8080

  spring:
    application:
      name: customer
    datasource:
      url: jdbc:postgresql://localhost:5432/customer
      username: tdksoft
      password: test

    jpa:
      hibernate:
        ddl-auto: create-drop
      properties:
        hibernate:
          format_sql: true
      show-sql: true


  stripe:
    enabled: false
